# ===================================
# PDF2BPMN 에이전트 서버 환경 설정
# ===================================
# 
# 사용법:
# 1. 이 파일을 'agent.env'로 복사: copy agent.env.example agent.env
# 2. 아래 값들을 실제 값으로 변경
# 3. 에이전트 실행: python pdf2bpmn_agent_server.py
#
# 주의: PDF2BPMN API 서버(.env)와 별도의 파일입니다!
# ===================================

# PDF2BPMN API 서버 URL
# run.py api로 실행한 서버 주소
PDF2BPMN_URL=http://localhost:8001

# Supabase 설정 (ProcessGPT와 동일한 값 사용)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SERVICE_ROLE_KEY=your-service-role-key-here

# 에이전트 설정
AGENT_ORCH=pdf2bpmn
POLLING_INTERVAL=5
TASK_TIMEOUT=3600

# 역할에 적절한 agent가 없을 때 자동으로 "agent user"를 생성할지 여부
# (요구사항: 에이전트가 필요한 경우 생성되어야 함)
ENABLE_AUTO_AGENT_CREATION=true
